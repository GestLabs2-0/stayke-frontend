/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { STAYKE_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CLEAR_ACTIVE_BOOKING_DISCRIMINATOR = new Uint8Array([
  109, 41, 40, 136, 10, 34, 239, 89,
]);

export function getClearActiveBookingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEAR_ACTIVE_BOOKING_DISCRIMINATOR
  );
}

export type ClearActiveBookingInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ClearActiveBookingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClearActiveBookingInstructionDataArgs = {};

export function getClearActiveBookingInstructionDataEncoder(): FixedSizeEncoder<ClearActiveBookingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLEAR_ACTIVE_BOOKING_DISCRIMINATOR,
    })
  );
}

export function getClearActiveBookingInstructionDataDecoder(): FixedSizeDecoder<ClearActiveBookingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClearActiveBookingInstructionDataCodec(): FixedSizeCodec<
  ClearActiveBookingInstructionDataArgs,
  ClearActiveBookingInstructionData
> {
  return combineCodec(
    getClearActiveBookingInstructionDataEncoder(),
    getClearActiveBookingInstructionDataDecoder()
  );
}

export type ClearActiveBookingInput<
  TAccountUserProfile extends string = string,
  TAccountAuthority extends string = string,
> = {
  userProfile: Address<TAccountUserProfile>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getClearActiveBookingInstruction<
  TAccountUserProfile extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: ClearActiveBookingInput<TAccountUserProfile, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): ClearActiveBookingInstruction<
  TProgramAddress,
  TAccountUserProfile,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.authority),
    ],
    data: getClearActiveBookingInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearActiveBookingInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountAuthority
  >);
}

export type ParsedClearActiveBookingInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: ClearActiveBookingInstructionData;
};

export function parseClearActiveBookingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClearActiveBookingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { userProfile: getNextAccount(), authority: getNextAccount() },
    data: getClearActiveBookingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
