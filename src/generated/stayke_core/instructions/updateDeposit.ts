/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { STAYKE_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const UPDATE_DEPOSIT_DISCRIMINATOR = new Uint8Array([
  126, 116, 15, 164, 238, 179, 155, 59,
]);

export function getUpdateDepositDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_DEPOSIT_DISCRIMINATOR,
  );
}

export type UpdateDepositInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateDepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  isDeposit: boolean;
};

export type UpdateDepositInstructionDataArgs = {
  amount: number | bigint;
  isDeposit: boolean;
};

export function getUpdateDepositInstructionDataEncoder(): FixedSizeEncoder<UpdateDepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["isDeposit", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_DEPOSIT_DISCRIMINATOR }),
  );
}

export function getUpdateDepositInstructionDataDecoder(): FixedSizeDecoder<UpdateDepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["isDeposit", getBooleanDecoder()],
  ]);
}

export function getUpdateDepositInstructionDataCodec(): FixedSizeCodec<
  UpdateDepositInstructionDataArgs,
  UpdateDepositInstructionData
> {
  return combineCodec(
    getUpdateDepositInstructionDataEncoder(),
    getUpdateDepositInstructionDataDecoder(),
  );
}

export type UpdateDepositInput<
  TAccountUserProfile extends string = string,
  TAccountAuthority extends string = string,
> = {
  userProfile: Address<TAccountUserProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  amount: UpdateDepositInstructionDataArgs["amount"];
  isDeposit: UpdateDepositInstructionDataArgs["isDeposit"];
};

export function getUpdateDepositInstruction<
  TAccountUserProfile extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: UpdateDepositInput<TAccountUserProfile, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateDepositInstruction<
  TProgramAddress,
  TAccountUserProfile,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.authority),
    ],
    data: getUpdateDepositInstructionDataEncoder().encode(
      args as UpdateDepositInstructionDataArgs,
    ),
    programAddress,
  } as UpdateDepositInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountAuthority
  >);
}

export type ParsedUpdateDepositInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateDepositInstructionData;
};

export function parseUpdateDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { userProfile: getNextAccount(), authority: getNextAccount() },
    data: getUpdateDepositInstructionDataDecoder().decode(instruction.data),
  };
}
