/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { STAYKE_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getPenaltySeverityDecoder,
  getPenaltySeverityEncoder,
  type PenaltySeverity,
  type PenaltySeverityArgs,
} from "../types";

export const ADD_INFRACTION_DISCRIMINATOR = new Uint8Array([
  238, 116, 210, 154, 186, 175, 136, 151,
]);

export function getAddInfractionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADD_INFRACTION_DISCRIMINATOR,
  );
}

export type AddInfractionInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountReputationProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReputationProfile extends string
        ? WritableAccount<TAccountReputationProfile>
        : TAccountReputationProfile,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type AddInfractionInstructionData = {
  discriminator: ReadonlyUint8Array;
  severity: PenaltySeverity;
};

export type AddInfractionInstructionDataArgs = {
  severity: PenaltySeverityArgs;
};

export function getAddInfractionInstructionDataEncoder(): FixedSizeEncoder<AddInfractionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["severity", getPenaltySeverityEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_INFRACTION_DISCRIMINATOR }),
  );
}

export function getAddInfractionInstructionDataDecoder(): FixedSizeDecoder<AddInfractionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["severity", getPenaltySeverityDecoder()],
  ]);
}

export function getAddInfractionInstructionDataCodec(): FixedSizeCodec<
  AddInfractionInstructionDataArgs,
  AddInfractionInstructionData
> {
  return combineCodec(
    getAddInfractionInstructionDataEncoder(),
    getAddInfractionInstructionDataDecoder(),
  );
}

export type AddInfractionInput<
  TAccountReputationProfile extends string = string,
  TAccountAuthority extends string = string,
> = {
  reputationProfile: Address<TAccountReputationProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  severity: AddInfractionInstructionDataArgs["severity"];
};

export function getAddInfractionInstruction<
  TAccountReputationProfile extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: AddInfractionInput<TAccountReputationProfile, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): AddInfractionInstruction<
  TProgramAddress,
  TAccountReputationProfile,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reputationProfile: {
      value: input.reputationProfile ?? null,
      isWritable: true,
    },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reputationProfile),
      getAccountMeta(accounts.authority),
    ],
    data: getAddInfractionInstructionDataEncoder().encode(
      args as AddInfractionInstructionDataArgs,
    ),
    programAddress,
  } as AddInfractionInstruction<
    TProgramAddress,
    TAccountReputationProfile,
    TAccountAuthority
  >);
}

export type ParsedAddInfractionInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    reputationProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: AddInfractionInstructionData;
};

export function parseAddInfractionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddInfractionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reputationProfile: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getAddInfractionInstructionDataDecoder().decode(instruction.data),
  };
}
