/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { STAYKE_CORE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_HOST_STATUS_DISCRIMINATOR = new Uint8Array([
  127, 70, 76, 106, 152, 218, 71, 254,
]);

export function getSetHostStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_HOST_STATUS_DISCRIMINATOR
  );
}

export type SetHostStatusInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountUserProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserProfile extends string
        ? WritableAccount<TAccountUserProfile>
        : TAccountUserProfile,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SetHostStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  status: boolean;
};

export type SetHostStatusInstructionDataArgs = { status: boolean };

export function getSetHostStatusInstructionDataEncoder(): FixedSizeEncoder<SetHostStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_HOST_STATUS_DISCRIMINATOR })
  );
}

export function getSetHostStatusInstructionDataDecoder(): FixedSizeDecoder<SetHostStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getBooleanDecoder()],
  ]);
}

export function getSetHostStatusInstructionDataCodec(): FixedSizeCodec<
  SetHostStatusInstructionDataArgs,
  SetHostStatusInstructionData
> {
  return combineCodec(
    getSetHostStatusInstructionDataEncoder(),
    getSetHostStatusInstructionDataDecoder()
  );
}

export type SetHostStatusInput<
  TAccountUserProfile extends string = string,
  TAccountAuthority extends string = string,
> = {
  userProfile: Address<TAccountUserProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  status: SetHostStatusInstructionDataArgs["status"];
};

export function getSetHostStatusInstruction<
  TAccountUserProfile extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAYKE_CORE_PROGRAM_ADDRESS,
>(
  input: SetHostStatusInput<TAccountUserProfile, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): SetHostStatusInstruction<
  TProgramAddress,
  TAccountUserProfile,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAYKE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userProfile: { value: input.userProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userProfile),
      getAccountMeta(accounts.authority),
    ],
    data: getSetHostStatusInstructionDataEncoder().encode(
      args as SetHostStatusInstructionDataArgs
    ),
    programAddress,
  } as SetHostStatusInstruction<
    TProgramAddress,
    TAccountUserProfile,
    TAccountAuthority
  >);
}

export type ParsedSetHostStatusInstruction<
  TProgram extends string = typeof STAYKE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: SetHostStatusInstructionData;
};

export function parseSetHostStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetHostStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { userProfile: getNextAccount(), authority: getNextAccount() },
    data: getSetHostStatusInstructionDataDecoder().decode(instruction.data),
  };
}
