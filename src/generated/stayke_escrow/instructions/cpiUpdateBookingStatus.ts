/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { STAYKE_ESCROW_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getBookingStatusDecoder,
  getBookingStatusEncoder,
  type BookingStatus,
  type BookingStatusArgs,
} from "../types";

export const CPI_UPDATE_BOOKING_STATUS_DISCRIMINATOR = new Uint8Array([
  225, 169, 181, 166, 175, 12, 138, 50,
]);

export function getCpiUpdateBookingStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CPI_UPDATE_BOOKING_STATUS_DISCRIMINATOR
  );
}

export type CpiUpdateBookingStatusInstruction<
  TProgram extends string = typeof STAYKE_ESCROW_PROGRAM_ADDRESS,
  TAccountBooking extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBooking extends string
        ? WritableAccount<TAccountBooking>
        : TAccountBooking,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type CpiUpdateBookingStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  status: BookingStatus;
};

export type CpiUpdateBookingStatusInstructionDataArgs = {
  status: BookingStatusArgs;
};

export function getCpiUpdateBookingStatusInstructionDataEncoder(): FixedSizeEncoder<CpiUpdateBookingStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getBookingStatusEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CPI_UPDATE_BOOKING_STATUS_DISCRIMINATOR,
    })
  );
}

export function getCpiUpdateBookingStatusInstructionDataDecoder(): FixedSizeDecoder<CpiUpdateBookingStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getBookingStatusDecoder()],
  ]);
}

export function getCpiUpdateBookingStatusInstructionDataCodec(): FixedSizeCodec<
  CpiUpdateBookingStatusInstructionDataArgs,
  CpiUpdateBookingStatusInstructionData
> {
  return combineCodec(
    getCpiUpdateBookingStatusInstructionDataEncoder(),
    getCpiUpdateBookingStatusInstructionDataDecoder()
  );
}

export type CpiUpdateBookingStatusInput<
  TAccountBooking extends string = string,
  TAccountAuthority extends string = string,
> = {
  booking: Address<TAccountBooking>;
  authority: TransactionSigner<TAccountAuthority>;
  status: CpiUpdateBookingStatusInstructionDataArgs["status"];
};

export function getCpiUpdateBookingStatusInstruction<
  TAccountBooking extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAYKE_ESCROW_PROGRAM_ADDRESS,
>(
  input: CpiUpdateBookingStatusInput<TAccountBooking, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): CpiUpdateBookingStatusInstruction<
  TProgramAddress,
  TAccountBooking,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    booking: { value: input.booking ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.booking),
      getAccountMeta(accounts.authority),
    ],
    data: getCpiUpdateBookingStatusInstructionDataEncoder().encode(
      args as CpiUpdateBookingStatusInstructionDataArgs
    ),
    programAddress,
  } as CpiUpdateBookingStatusInstruction<
    TProgramAddress,
    TAccountBooking,
    TAccountAuthority
  >);
}

export type ParsedCpiUpdateBookingStatusInstruction<
  TProgram extends string = typeof STAYKE_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    booking: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: CpiUpdateBookingStatusInstructionData;
};

export function parseCpiUpdateBookingStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCpiUpdateBookingStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { booking: getNextAccount(), authority: getNextAccount() },
    data: getCpiUpdateBookingStatusInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
