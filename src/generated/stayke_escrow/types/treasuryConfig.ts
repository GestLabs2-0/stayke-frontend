/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Config account for the Treasury program.
 * Stores the authority allowed to manage treasury operations
 * and the references to the on-chain token vaults.
 */
export type TreasuryConfig = {
  /** The admin/authority pubkey that can manage the treasury. */
  authority: Address;
  /**
   * Pubkey of the treasury token account (controlled by treasury_pda).
   * Holds user USDC guarantee deposits.
   */
  treasuryVault: Address;
  /** Bump of the treasury PDA ([b"treasury"]) — used to sign CPI calls. */
  treasuryBump: number;
  /** The USDC mint accepted by this treasury. */
  usdcMint: Address;
  /** Minimum deposit amount (in USDC lamports) required from each user. */
  minimumDeposit: bigint;
  /** Guard flag to prevent re-initialization. */
  isInitialized: boolean;
  bump: number;
};

export type TreasuryConfigArgs = {
  /** The admin/authority pubkey that can manage the treasury. */
  authority: Address;
  /**
   * Pubkey of the treasury token account (controlled by treasury_pda).
   * Holds user USDC guarantee deposits.
   */
  treasuryVault: Address;
  /** Bump of the treasury PDA ([b"treasury"]) — used to sign CPI calls. */
  treasuryBump: number;
  /** The USDC mint accepted by this treasury. */
  usdcMint: Address;
  /** Minimum deposit amount (in USDC lamports) required from each user. */
  minimumDeposit: number | bigint;
  /** Guard flag to prevent re-initialization. */
  isInitialized: boolean;
  bump: number;
};

export function getTreasuryConfigEncoder(): FixedSizeEncoder<TreasuryConfigArgs> {
  return getStructEncoder([
    ["authority", getAddressEncoder()],
    ["treasuryVault", getAddressEncoder()],
    ["treasuryBump", getU8Encoder()],
    ["usdcMint", getAddressEncoder()],
    ["minimumDeposit", getU64Encoder()],
    ["isInitialized", getBooleanEncoder()],
    ["bump", getU8Encoder()],
  ]);
}

export function getTreasuryConfigDecoder(): FixedSizeDecoder<TreasuryConfig> {
  return getStructDecoder([
    ["authority", getAddressDecoder()],
    ["treasuryVault", getAddressDecoder()],
    ["treasuryBump", getU8Decoder()],
    ["usdcMint", getAddressDecoder()],
    ["minimumDeposit", getU64Decoder()],
    ["isInitialized", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

export function getTreasuryConfigCodec(): FixedSizeCodec<
  TreasuryConfigArgs,
  TreasuryConfig
> {
  return combineCodec(getTreasuryConfigEncoder(), getTreasuryConfigDecoder());
}
