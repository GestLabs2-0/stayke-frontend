/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import { STAYKE_TREASURY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const WITHDRAW_FROM_LENDING_DISCRIMINATOR = new Uint8Array([
  11, 191, 231, 168, 105, 209, 8, 166,
]);

export function getWithdrawFromLendingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_FROM_LENDING_DISCRIMINATOR
  );
}

export type WithdrawFromLendingInstruction<
  TProgram extends string = typeof STAYKE_TREASURY_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawFromLendingInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type WithdrawFromLendingInstructionDataArgs = {
  amount: number | bigint;
};

export function getWithdrawFromLendingInstructionDataEncoder(): FixedSizeEncoder<WithdrawFromLendingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: WITHDRAW_FROM_LENDING_DISCRIMINATOR,
    })
  );
}

export function getWithdrawFromLendingInstructionDataDecoder(): FixedSizeDecoder<WithdrawFromLendingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getWithdrawFromLendingInstructionDataCodec(): FixedSizeCodec<
  WithdrawFromLendingInstructionDataArgs,
  WithdrawFromLendingInstructionData
> {
  return combineCodec(
    getWithdrawFromLendingInstructionDataEncoder(),
    getWithdrawFromLendingInstructionDataDecoder()
  );
}

export type WithdrawFromLendingInput<TAccountSigner extends string = string> = {
  signer: TransactionSigner<TAccountSigner>;
  amount: WithdrawFromLendingInstructionDataArgs["amount"];
};

export function getWithdrawFromLendingInstruction<
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof STAYKE_TREASURY_PROGRAM_ADDRESS,
>(
  input: WithdrawFromLendingInput<TAccountSigner>,
  config?: { programAddress?: TProgramAddress }
): WithdrawFromLendingInstruction<TProgramAddress, TAccountSigner> {
  // Program address.
  const programAddress =
    config?.programAddress ?? STAYKE_TREASURY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.signer)],
    data: getWithdrawFromLendingInstructionDataEncoder().encode(
      args as WithdrawFromLendingInstructionDataArgs
    ),
    programAddress,
  } as WithdrawFromLendingInstruction<TProgramAddress, TAccountSigner>);
}

export type ParsedWithdrawFromLendingInstruction<
  TProgram extends string = typeof STAYKE_TREASURY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
  };
  data: WithdrawFromLendingInstructionData;
};

export function parseWithdrawFromLendingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedWithdrawFromLendingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount() },
    data: getWithdrawFromLendingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
